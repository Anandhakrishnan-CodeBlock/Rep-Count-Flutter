import 'package:flutter/material.dart';

class CounterActionScreen extends StatelessWidget {
  const CounterActionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}